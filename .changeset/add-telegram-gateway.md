---
"@jmfederico/pi-web": minor
---

Add a Telegram Gateway plugin package with an in-app setup panel, Telegram-user allowlist rows that attach the BotFather token for the bot each user/session should use, start/stop controls, live config reload for added user/bot rows without manual gateway restarts, setup wizard, secret-free example config, documentation, and a dependency-free Node.js long-polling gateway. Add a Workspace Auth plugin with setup wizard, shared access-policy template, Clerk JWT verification using Node built-ins, and server-side workspace ACL core so Telegram IDs can optionally link to Clerk user IDs and inherit assigned workspaces.
